<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.fecapads.fecapassjava">
    <!-- Permissão para acessar a Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FecapassJava"
        tools:targetApi="31">
        <activity
            android:name=".Pix"
            android:exported="false" />
        <activity
            android:name=".FormaDePagamento"
            android:exported="false" />
        <activity
            android:name=".SelecaoCompra"
            android:exported="false" />
        <activity
            android:name=".EventoFecapejada"
            android:exported="false" /> <!-- Atividades do app -->
        <activity android:name=".TelaEntrar" />
        <activity android:name=".EsqueciSenha" />
        <activity android:name=".CadastroResultado" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>